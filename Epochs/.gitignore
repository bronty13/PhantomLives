node_modules/
out/
dist/
.vite/
*.log
*.tsbuildinfo
.DS_Store
build/icon.icns
build/icon.png
build/icon.iconset/

# Owner's scanned rulebook pages — bundled into the local app build, never committed
src/renderer/public/rulebook/

# Copyrighted board scan — basemap for the local build, never committed (owner's own scan)
art/board-source.*
art/board-crop.*
src/renderer/public/board.jpg
