# Changelog

The canonical changelog lives at the repo root: **[../CHANGELOG.md](../CHANGELOG.md)**.

This file used to mirror release notes alongside the other docs; in 1.0.0 it
became a stub so there is exactly one source of truth, auto-maintained by
the `pre-commit` hook installed by `scripts/install-git-hooks.sh`.
