name: MusicJournal
options:
  bundleIdPrefix: com.bronty
  deploymentTarget:
    macOS: "14.0"
  xcodeVersion: "16"

packages:
  GRDB:
    url: https://github.com/groue/GRDB.swift
    from: "6.0.0"

targets:
  MusicJournal:
    type: application
    platform: macOS
    sources:
      - path: Sources/MusicJournal
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.bronty.MusicJournal
        SWIFT_VERSION: "5.10"
        MACOSX_DEPLOYMENT_TARGET: "14.0"
        CODE_SIGN_STYLE: Automatic
        INFOPLIST_FILE: Sources/MusicJournal/App/Info.plist
        CODE_SIGN_ENTITLEMENTS: Sources/MusicJournal/App/MusicJournal.entitlements
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
    dependencies:
      - package: GRDB
    scheme:
      testTargets: []
