/// Central version constants — update here and in Info.plist together.
enum AppVersion {
    static let marketing = "1.0.0"
    static let build = "1"

    /// Human-readable string shown in About and Settings panels.
    static let display = "v\(marketing) (\(build))"
}
