import XCTest

/// Placeholder so the `PurplePeekTests` target has a valid source path in Phase 1. The real
/// suite (DatabaseTests / MediaDiscoveryTests / BackupTests) lands in Phase 7.
final class SmokeTests: XCTestCase {
    func testHarnessRuns() {
        XCTAssertTrue(true)
    }
}
