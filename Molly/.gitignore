node_modules
dist
dist-ssr
*.local
.vite
*.tsbuildinfo

# Tauri
src-tauri/target
src-tauri/gen

# (Icons in src-tauri/icons/ are committed; they're generated from molly.svg
# via scripts/gen-icons.sh but kept in source control so first builds work.)

# OS
.DS_Store
Thumbs.db

# Editor
.idea
.vscode/*
!.vscode/extensions.json
*.swp

# Bundled ffmpeg/ffprobe static binaries: CI downloads them per-platform
# before `tauri build`; never commit (~80-100 MB). Keep the dir via .gitkeep.
src-tauri/resources/ffmpeg/*
!src-tauri/resources/ffmpeg/.gitkeep
